{
  "name": "itranslate",
  "version": "1.4.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "bump": "node scripts/bump-build.js",
    "build": "tsc && vite build && vite build --config vite.content.config.ts",
    "release": "npm run bump && npm run build && node scripts/package-zip.cjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "idb": "^8.0.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.270",
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^24.0.0",
    "puppeteer": "^25.1.0",
    "sharp": "^0.34.5",
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
